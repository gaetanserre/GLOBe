#set page(width: auto, height: auto)

#set text(font: "New Computer Modern", size: 12pt)

#grid(
  columns: 3,
  image("Bentcigar.svg") + align(center, [Bent cigar]),
  image("Dixonprice.svg") + align(center, [Dixen Price]),
  image("Hyperellipsoid.svg") + align(center, [Hyper-Ellipsoid]),

  image("Rosenbrock.svg") + align(center, [Rosenbrock]),
  image("Square.svg") + align(center, [Square]),
  image("Sumpow.svg") + align(center, [Sumpow]),

  image("Trid.svg") + align(center, [Trid]), image("Zakharov.svg") + align(center, [Zakharov]), [],
)
